const fetchNui = async () => {
  return 1;
};
const makeThing = function() {};
const values = [1].map(() => 1);

class GrandParent {
  parent() {
    function childHelper() {}
    return childHelper;
  }
}
